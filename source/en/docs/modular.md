---
title: Modularization in Doric
---

Doric projects can be split into panels, reusable view components, and extension libraries.

## Guidelines

- Split pages by feature.
- Extract repeated UI into components.
- Use `createLib` for native extension modules.
